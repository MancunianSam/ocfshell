name: Release OCFShell
on:
  push:
    branches:
      - main
permissions:
  contents: write
  pull-requests: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 #v6.0.3
      - id: next-tag
        run: echo next-version=$(curl -s $GITHUB_API_URL/repos/MancunianSam/ocfshell/tags | jq -r '.[0].name' | awk -F. -v OFS=. '{ $3++; print }') >> "$GITHUB_OUTPUT"
      - run: |
          sudo apt-get install musl-tools
          rustup target add x86_64-unknown-linux-musl
          cargo build --release --target x86_64-unknown-linux-musl
          git tag ${{ steps.next-tag.outputs.next-version }}
          git push origin ${{ steps.next-tag.outputs.next-version }}
          gh release create ${{ steps.next-tag.outputs.next-version }} target/x86_64-unknown-linux-musl/release/ocfshell
        env:
          GH_TOKEN: ${{ github.token }}
  documentation-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 #v6.0.3
        with:
          fetch-depth: '0'
      - run:  |
          cargo doc --no-deps --workspace --document-private-items
          git config --global user.email 181243999+tna-da-bot@users.noreply.github.com 
          git config --global user.name tna-da-bot
          git checkout gh-pages
          rm -rf docs
          mv target/doc/ docs
          git add docs
          git commit -m "Update site documentation"
          git push -u origin gh-pages
        env:
          GH_TOKEN: ${{ github.token }}